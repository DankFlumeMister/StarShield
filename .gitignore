# --- OS ---
Thumbs.db
Desktop.ini
.DS_Store
ehthumbs.db

# --- Editors ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- KiCad ---
*.lck
*.kicad_prl
*-backups/
fp-info-cache
*.bak
*.kicad_pcb-bak
*.sch-bak
_autosave-*

# --- Build outputs ---
build/
*.uf2
*.hex
*.bin
*.elf
*.map
*.o
*.d

# --- ZMK / Zephyr ---
.zmk/
.zephyr/

# --- 3D printing (slicer outputs) ---
*.gcode
*.3mf

# --- Python (helper scripts) ---
__pycache__/
*.pyc

# --- 根目录下的一次性临时脚本（用 _ 前缀；正式脚本放 docs/_tools/）---
/_*.mjs
/_*.cjs
/_*.js
/_*.py
/_*.txt

# --- 根目录抓取/调试产物（调研工具常落在这里）---
# 真实事故：一次 `git add -A` 把第三方三模键盘工程（257 文件）、一个 10 MB zip、
# 几个 .ps1 与 .html 提交并推到了公开仓库。提交前务必跑
#   python docs/_tools/check_repo_tree.py --staged
# 该脚本用【白名单】校验顶层条目，能挡住「名字事先想不到的新目录」。
/*.ps1
/*.zip
/*.html
/*.htm
/*.json
/*.txt
/*.pdf
/*.png
/*.jpg

# --- Node / JS dependencies & caches（绝不入库）---
node_modules/
npmcache/
_cacache/
package-lock.json
*.log

# --- 调研工作区（第三方页面/PDF/克隆的仓库/图片/一次性脚本）---
# 采用【白名单】策略：research/ 下只保留结论性的 .md 报告，其余一律不入库。
# 为什么不用逐个后缀枚举：枚举必然漏（曾漏掉 *.png / *.py / 任意名字的克隆目录），
# 而本仓库是公开的，漏一个就会把无许可证的第三方原理图/规范原文推上去。
# 白名单策略下，新增任何临时产物都默认安全。
research/*
!research/*.md

# --- 调研临时工作目录（工具/子代理的一次性产出）---
.research/
.zmk-research/
_research/
_k2dis/
_k2man/
_latency_research/
_vic84/
_zf/
_zmkguide/

# --- Secrets / privacy ---
*.env
secrets/
personal/
private/

# --- 内部文件（不随开源仓库发布）---
PROJECT_HANDOFF.md
PROJECT_HANDOFF_HISTORY.md
docs/_internal/

# 交接层里那两份**未清洗的原始调研报告**：清洗版已作为 research/*.md 入库，
# 根目录这两份含子代理原始措辞、不得发布。⚠️ 它们**未被忽略时**是 `git add -A` 的地雷
# （本仓库已经因为裸 `git add -A` 误推过第三方文件一次），故逐名钉死。
/hardware-research-report.md
/zmk-tri-mode-community-precedent.md

# --- 生成器的中间产物 ---
# `docs/_generated/` 里**只有 matrix.json 入库**（它是生成链的输入真相源，已跟踪）。
# 其余都是「生成器 → merge 脚本」的一次性中间文件，例如 gen_matrix_doc.py 产出的
# matrix-doc-tables.md（由 merge_matrix_doc.py 合并进 docs/matrix-assignment.md）。
# 这类文件每次重跑生成器都会重新出现 ⇒ 不忽略的话 git status 永远不干净，
# 而「重跑生成器后 git status 对 tracked 文件无差异」正是本项目的确定性判据。
docs/_generated/*
!docs/_generated/matrix.json

# --- 第三方参考源（无许可证，仅本地学习，不入库）---
hardware/pcb/reference/

# --- 调研临时工作区（抓取的第三方页面/PDF/一次性脚本，不入库）---
_jst/
