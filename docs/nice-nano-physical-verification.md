# B6：nice!nano v2 采购与实物核对清单

> 目标：**投板前**用真实模块核对两个决定性尺寸 —— ① 排针（pitch / 每边脚数），
> ② 板载 USB-C 座相对板边的位置与朝向（决定外壳开孔）。
> 本文件分两部分：**拿货前的桌面预研基准值**（分来源与可信级）+ **拿到实物后的逐项量测清单**
> （每项给出量法、预判值、偏差处置）。
> ⚠️ 所有「预判值」均来自旁证（社区封装库 / 第三方模型 / 零售商页面），
> **在 §3 打勾之前不得当作事实写进外壳图纸**（handoff §10.2 第 9 条：实测与旁证必须分开记）。

状态：🟡 **实物已到货，量测已完成大半（2026-09-19）**——§6 是量测结论，
**发现 J3A/J3B 孔数设计错误（12 → 应为 13），详见 §6.1，修复方案见 §6.3**。

---

## 1. 采购清单

| 物料 | 数量 | 用途 | 备注 |
| --- | --- | --- | --- |
| **nice!nano v2** | **2 块** | 1 块装机 + 1 块做 C7 ESB 端点验证 | 官方渠道 nicekeyboards.com（约 $25）；国内电商有售时注意甄别**兼容板**——SuperMini 等克隆板已被 ADR-0003 降级为「焊盘兼容、不保证」，**克隆板待机电流批次差异极大**（好板 4 µA / 差板 60 µA，见 `controller-and-battery-facts.md` §1.1） |
| **XIAO nRF52840** | 1 块 | C7 ESB 验证（决定 Dongle 方案 A/B） | 物料与判据见 `docs/esb-endpoint-validation.md` |
| **12-pin 排针** | 随模块附 2 条 | 插到我们 PCB 的 J3A/J3B 母座上 | nice!nano v2 包装内含 2× 12-pin + 1× 3-pin 排针；**收货时清点** |
| USB-C **数据线** | 1 条 | 刷固件 | ⚠️ 必须是数据线，纯充电线刷不了 UF2 |
| 游标卡尺（0–150 mm） | 1 把 | 本清单全部量测 | 精度 0.02 mm 足够；没有卡尺用直尺只能做 §3 的 A2（11 个间隔合计 27.94 mm，误差会被摊薄到可判读） |

我们 PCB 这边的配套件（已在设计内，仅提醒采购主板物料时别漏）：J3A/J3B = `PinSocket_1x12_P2.54mm` 立式母座 ×2。

---

## 2. 桌面预研基准值（拿货前的旁证汇总）

### 2.1 排针（决定我们 J3A/J3B 是否插得进去）

| 项 | 预判值 | 来源与可信级 |
| --- | --- | --- |
| 每边脚数 | ~~12~~ → **13**（❌ **预判已被实物证伪**，见 §6.1） | marbastlib 封装只画了「Pro Micro 兼容 12 孔」子集、零售商只写「2x 12-pin headers」——**两处旁证都数漏了 USB 端多出的电源孔**；官方 pinout 图（nicekeyboards.com）+ 实物清点均 = 13 |
| pitch | **2.54 mm** | 同上 + Pro Micro 标准 ⇒ **可信** |
| 两列间距 | **15.24 mm** | marbastlib 封装 pad x = ±7.62；**我们 PCB 的 J3A(y=4.38)/J3B(y=19.62) 正是按 15.24 布的** ⇒ 互相印证 |
| 排针针位跨度 | **27.94 mm**（11 × 2.54） | marbastlib pad y = −13.97…+13.97 |

### 2.2 板外形（三处来源**互相矛盾** ⇒ 必须实物定案）

| 来源 | 板长 | 板宽 | 厚 |
| --- | --- | --- | --- |
| marbastlib 封装轮廓（Pro Micro 标准） | **33.02** | 17.78 | — |
| 零售商页面（mechboards） | **34.1**（疑含 USB 座凸出） | 18.3 | 3.3（总高）/ PCB 1.6 |
| 第三方 STEP 模型（marathon-os） | **34.00** | 17.80 | 3.16（总高） |

⇒ 长度 33.02 vs 34.1 差 **1.1 mm**：直接影响「USB 座口到键盘板边的距离」与外壳开孔 ⇒ **以卡尺实测定案**。

### 2.3 板载 USB-C 座（决定外壳开孔的一切）

| 项 | 预判值 | 来源 |
| --- | --- | --- |
| 形态 | **mid-mount**（座子嵌在板边内），官方口径 3.2 mm 总高 | 零售商 / openhardware.directory，一致 ⇒ 可信 |
| 座子 X 向位置 | **板宽方向居中** | marbastlib（USB 注释块 −3.55…+3.55）+ nRF52840-promicro 逆向件（X 居中）⇒ 双旁证 |
| 座子口面伸出板边 | **约 1.52 mm**（座体宽约 7.1 mm） | marbastlib Cmts.User 矩形（−19.3…−14.2 vs 板边 −17.78），**单旁证** |
| 座子在板长方向的位置 | 距 pin 1 端约 **2.8 mm**（座中心）、口面距 pin1 端约 5.3 mm | 同上 |

### 2.4 天线（顺手核对，不阻塞投板）

marbastlib 在封装里画了一块 `ANT` keepout：**天线在 USB 的对侧（pin 12/24 端）**，
沿板长方向伸出板边约 5 mm、横向超出板宽约 ±5 mm。
⇒ 插到我们板上后，天线区大致覆盖键盘板 **x ≈ 50–60 mm、y ≈ 0–26 mm**（模块中线 y=12）。
该区域下方是我们 In1（GND 平面）/ In2（VLED 平面）/ B.Cu（GND 铺铜）——
「是否给天线挖空铺铜」属旁证级建议、无强规范 ⇒ 记为 **B6 之后的决策项**，量测时顺手记下天线实际位置即可。

---

## 3. 拿到实物后的量测清单（逐项打勾）

> 量法约定：pitch 不要单个量（2.54 mm 手抖就废），**量首尾两个针的总长再除以间隔数**。

### A. 排针（插不进 = 全盘返工，最先量）

| # | 项 | 量法 | 预判 | 判据 / 偏差处置 |
| --- | --- | --- | --- | --- |
| A1 | 每边脚数 | 数 | 12 + 12 | ≠12 ⇒ 停：J3A/J3B 封装与外壳全要改，回报再议 |
| A2 | pitch | 量 pin1→pin12 总长 ÷ 11 | 27.94 mm | ±0.2 mm 内 ⇒ 合格；超差 ⇒ 量 pin1→pin2 单距复核，回报 |
| A3 | 两列间距 | 量两条排针中心距 | 15.24 mm | ±0.2 mm；超差 ⇒ J3A/J3B 坐标要改（`pcb_design.py`） |
| A4 | 插入验证 | 把模块（焊好排针）**真的插进样板或母座** | 顺滑到位 | 歪斜/过松 ⇒ 换母座型号；此步最晚在投板前用面包板母座即可做 |

### B. USB-C 座（决定外壳开孔）

| # | 项 | 量法 | 预判 | 判据 / 偏差处置 |
| --- | --- | --- | --- | --- |
| B1 | **USB 在哪一端** | 目视：USB 端是 pin1（TX0/标号 D1 侧）还是 pin24（RAW） | **pin 1 端**（Pro Micro 惯例 + marbastlib） | 若在 pin24 端 ⇒ §4 预判落点整体镜像到 x≈73 mm，外壳开孔重新算 |
| B2 | 座子中心距 pin1 端板边 | 卡尺 | 板边在 pin1 外 3.81 mm，座中心再进 2.8 mm | 记实测值，代入 §4 公式 |
| B3 | 口面伸出量 | 卡尺量座口到板边 | 伸出约 1.5 mm（mid-mount） | 缩进板内则开孔要贴紧板边、余量更小 |
| B4 | 座子口宽 | 卡尺 | 约 7.1 mm | 开孔按「口宽 + 2 mm」起步 |

### C. 板外形（定外壳）

| # | 项 | 预判 | 处置 |
| --- | --- | --- | --- |
| C1 | 板长 × 板宽 × PCB 厚 | 见 §2.2 三来源矛盾，**实测定案** | 取代 §2.2 全部旁证 |
| C2 | 总高（含元件、USB 座） | 约 3.2–3.3 mm | 进外壳底壳净空与电池仓深度核算 |

### D. 顺手记录

| # | 项 | 预判 | 用途 |
| --- | --- | --- | --- |
| D1 | 天线实际位置（哪一端、距板边） | USB 对侧 | §2.4 的铺铜决策 |
| D2 | 板载充电档跳线位置 | 100/500 mA 焊接选择 | 我们电池 10 Ah，**确认置于 500 mA 档**（虽由 BQ24072 主充，模块档位影响 USB 直插调试时的安全） |
| D3 | 蓝灯（P0.15）/ VCC 门控（P0.13）引脚与丝印 | 官方口径 | 与固件假设复核（`verify_firmware.py` 已按此两脚留空） |

---

## 4. USB 落点：预判 → 实测修正后的最终值（B6-1 改版后）

我们板上 J3A / J3B（改造后 `PinSocket_1x13`，rot=90）几何（解析 `Starshield.kicad_pcb`）：

- J3A：孔1 世界坐标 **(37.46, 4.38)**，孔13 **(67.94, 4.38)** —— 针位沿 X 向，pitch 2.54
- J3B：孔1 **(37.46, 19.62)**，孔13 **(67.94, 19.62)** —— 两列间距 15.24 ✅
- 模块中线 **y = 12.0 mm**（距键盘板上边缘 12 mm）
- ⚠️ 改造前是 1×12、首孔在 x=40；**2026-09-19 B6-1 起首孔移到 x=37.46**
  （新增的正是 USB 端那第 13 孔，见 §6.2/§6.3）

按**实测**（孔1 → USB 口面 = 5.3 mm，实物卡尺）：

| 量 | 值（键盘板坐标系，原点 = 板左上角） |
| --- | --- |
| **USB 座口面** | **x ≈ 32.2 mm，y ≈ 12 ± 3.6 mm，朝 −X（键盘左侧）** |
| 模块板边（USB 端） | x ≈ 37.46 − lip A（lip A = 孔1 到板边距离，待补量测） |

⚠️ **必须正视的机械问题**：座口距键盘板左边缘约 **32.2 mm**。
「底壳侧壁开孔」方案（`power-architecture.md` §4）下，插线时要横穿这 32 mm 的壳内空间
—— **常见 USB-C 插头 tip→线缆段约 30–45 mm，能不能隔着壳壁插到位是临界情况**。
请**用实物 + 一条普通数据线**实际比划（模拟壳壁挡板），并把结论记回
`power-architecture.md` §4。若不可行，备选：外壳在 USB 口上方开**底壳开槽**（备选方案②）、
或下一代板把 J3A/J3B 旋转 90°（USB 朝上边沿，代价是控制肩重新布件）。
**在核实之前，不要把「侧壁开孔」当成已定事实写进外壳模型。**

---

## 5. 完成后要回写的地方（防旧值残留）

- [ ] 本文件 §3 全部打勾 + 实测值表
- [ ] `controller-and-battery-facts.md` §1.1「主排针布局」行（去掉 ❓）
- [ ] `power-architecture.md` §4 开孔段（定孔位坐标）
- [ ] `PROJECT_HANDOFF.md` §8 表「nice!nano 排针」行 + §9.1 B6 条目
- [ ] 外壳建模（G4）启动前，把 §4 落点换算成壳体开孔坐标

---

## 6. 量测结论（2026-09-19，实物到货当日）

### 6.1 已确认的实测值

| 项 | 实测 | 与预判对比 |
| --- | --- | --- |
| 板长 | **34.0 mm** | 取 34.0/34.1 一侧 ✅（marbastlib 的 33.02 系 Pro Micro 兼容子集，偏小） |
| 板宽 | **18.0 mm** | 取 18.3/17.8 之间 ✅ |
| pin1 → USB 口面 | **5.3 mm** | 与 marbastlib 预判 5.3 **完全一致** ✅ |
| USB 座口宽 | **7.1 mm，板宽居中** | 与预判一致 ✅ |
| 总高 | **3.3 mm** | 预判 3.2–3.3 ✅ |
| **每边孔数** | **13** | ❌ **预判 12 错误**（旁证双重误导：marbastlib 封装与零售商都按 Pro Micro 2×12 抄） |

### 6.2 🔴 发现的设计错误：J3A/J3B 必须从 1×12 改为 1×13

官方 pinout（nicekeyboards.com 原图，本机留档 `.workbuddy/scratch/pinout-v2-a6d36.png`）
确认 nice!nano v2 = **2×13 = 26 孔**，比 Pro Micro 每排多 1 孔——**多在 USB 端**：
左排顶端多 1 个 GND、右排顶端多 1 个 B+（故右排 B+ 有两个孔）。
这也解释了板长 34.0 ≠ Pro Micro 33.02。
（21 GPIO 口径不变：主排针 18 个 D 脚 + 背面 3 个焊盘 P1.01/P1.02/P1.07。）

**对现设计的影响**（`control_design.py` 的映射逐孔对照官方顺序）：

- 左排：我们的 J3A.1（LED_DIN = D1/P0.06）会插到实物的 **GND 孔**上 —— **整排错位一孔**；
- 右排：J3B.1（OUT→RAW/B+）恰好对上 B+ #1（侥幸），但 J3B.4（VCC）会插到实物的
  **RESET 孔**上 —— **VCC 短接复位，致命**；
- **固件 overlay 无需改动**：所有 D 编号（D2/D14/D18 模式、D19/D15/D16 五95、D4–D9 行、
  D1 RGB 数据、D10 门控）与 ZMK gpio-map 的对应关系不变，变的只是物理孔位。

**修正后的插座映射**（新 J3A/J3B = 1×13，编号 = 实物孔位序号，自 USB 端起）：

| J3A（左排） | 网 | | J3B（右排） | 网 |
| --- | --- | --- | --- | --- |
| 1 | GND | | 1 | OUT（B+ #1） |
| 2 | LED_DIN（D1/P0.06） | | 2 | OUT（B+ #2，同网冗余） |
| 3 | NC（D0/P0.08） | | 3 | GND |
| 4 | GND | | 4 | NC（RESET） |
| 5 | GND | | 5 | VCC（3.3V） |
| 6 | MODE0（D2/P0.17） | | 6 | NC（D21，ADC 保留） |
| 7 | NC（D3/P0.20） | | 7 | NC（D20，ADC 保留） |
| 8 | ROW0（D4/P0.22） | | 8 | 595_RCLK（D19/P0.02） |
| 9 | ROW1（D5/P0.24） | | 9 | MODE2（D18/P1.15） |
| 10 | ROW2（D6/P1.00） | | 10 | 595_SCK（D15/P1.13） |
| 11 | ROW3（D7/P0.11） | | 11 | MODE1（D14/P1.11） |
| 12 | ROW5（D8/P1.04） | | 12 | 595_MOSI（D16/P0.10） |
| 13 | ROW4（D9/P1.06） | | 13 | RGB_PWR_EN（D10/P0.09） |

### 6.3 修复方案与执行结果（✅ 2026-09-19 已完成）

**关键洞察（决定了整个修法的简单程度）**：官方孔序去掉第 1 孔后，与我们现有 12 个焊盘的网
**逐孔完全一致**（已用脚本逐项核对）：

```
J3A  official[1:] == 现有 [LED_DIN, NC, GND, GND, MODE0, NC, ROW0..ROW3, ROW5, ROW4]  ✓
J3B  official[1:] == 现有 [OUT, GND, NC, VCC, NC, NC, 595_RCLK, MODE2, 595_SCK,
                          MODE1, 595_MOSI, RGB_PWR_EN]                                ✓
```

⇒ **只需在 USB 端左侧（x = 37.46）加一个孔**（J3A 加 GND、J3B 加 OUT），
再把 Pad 号整体后移一位 —— **走线一根都不用动**：

| 步骤 | 内容 | 结果 |
| --- | --- | --- |
| 1 | 符号缓存加 `Conn_01x13`（`extract_power_symbols.py` 的 WANTED 表） | ✅ |
| 2 | `control_design.py`：J3A/J3B 改 `Conn_01x13`、CONN 表按官方孔序重排、页脚注更新 | ✅ |
| 3 | `gen_control_sch.py` 重生成控制子图（版式自检通过） | ✅ |
| 4 | 校验器断言同步（`verify_control` / `verify_rgb`，孔号 + LED_DIN 落孔） | ✅ |
| 5 | PCB：加第 13 孔 + 重排 Pad 号 + J3B 孔1-孔2 的 OUT 同网桥 + 重填铺铜 | ✅ |
| 6 | DRC + 七项校验器全绿；生成链确定性复核（重跑后 git status 无变化） | ✅ |

**❌ 已否决的做法（勿重试）**：把「网络整体后移一孔」、只移动走线端点 ——
实测撞出 **8 处 `shorting_items` + 2 处 `tracks_crossing` + 1 处 `clearance`**
（走线变成斜线后穿过邻孔、且要连带搬过孔）。**加一孔方案才是唯一正解。**

⚠️ **本次执行中发生过一次严重事故（已修复）**：迁移脚本用了
`open(P,"wb").write(open(P,"rb").read()...)` 的单行写法 —— `"wb"` 先把文件截断成 0 字节，
读回来自然是空的 ⇒ **6.5 MB 板文件被写成 0 字节**。靠落盘前的备份救回，
正确写法必须先读进变量再写（见 handoff §10.2 新增条目）。

### 6.4 仍待补的量测（不阻塞原理图修复）

| 项 | 用途 |
| --- | --- |
| 第 1 孔中心 → USB 侧板边（lip A）、第 13 孔中心 → 远端板边（lip B） | 校验 A + 30.48 + B = 34.0；推导 USB 口伸出量（= 5.3 − lip A，预判 1.5，但 13 孔下 lip 总和只有 ~3.5，**伸出量可能明显大于 1.5，直接影响开孔深度**） |
| 两排孔中心距（预判 15.24） | 确认 J3A/J3B 相对位置无需改 |
| pin1 → pin13 实距（预判 30.48） | 确认孔距确为 2.54 |
| 数据线插头 ℓ（套管前端 → 尖端） | 判定 §4 的 34.7 mm 插线临界问题 |
